package com.sarginsonconsulting.sbt


import org.scalatest.{FlatSpec, Matchers}

//import clojure.java.api.Clojure
//import clojure.lang.IFn


class TddReporterSpec extends FlatSpec with Matchers {


  "calling clojure" should "be awesome" in {

    //val require: IFn = Clojure.`var`("clojure.core", "require")
    //require.invoke(Clojure.read("snapshots.snapshot-saver"))
    //require.invoke(Clojure.read("data.in-memory"))

    //val recordSnapshot: IFn = Clojure.`var`("snapshots.snapshot-saver", "record-snapshot")
    //recordSnapshot.invoke(new java.util.Date, 4, "davesTestFile.clj")
    //recordSnapshot.invoke(new java.util.Date, 2, "davesTestFile2.clj")
    //recordSnapshot.invoke(new java.util.Date, 1, "davesTestFile.clj")

    
    //val stats: IFn = Clojure.`var`("data.in-memory", "output-data")
    //stats.invoke()

  }
}
